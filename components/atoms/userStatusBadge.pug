mixin userStatusBadge(type)
  if type && type.trim() !== ''
    -
      let badgeColor = '';
      switch(type.toLowerCase()) {
        case 'админ':
          badgeColor = 'bg-lab-red';
          break;
        case 'модератор':
          badgeColor = 'bg-lab-color';
          break;
        case 'админ клуба':
          badgeColor = 'bg-lab-green';
          break;
        case 'организатор':
          badgeColor = 'bg-lab-blue';
          break;
        case 'тренер':
          badgeColor = 'bg-lab-yellow';
          break;
        case 'фотограф':
          badgeColor = 'bg-purple-500';
          break;
        case 'стрингер':
          badgeColor = 'bg-lab-black';
          break;
      }
    div(class=`${badgeColor} z-10 w-fit rounded px-1 py-0.5 text-xs font-medium text-white`)
      | #{ type }
